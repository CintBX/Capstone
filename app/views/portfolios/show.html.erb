<div class="container portfolio-container">
	<div class="row">
		<div class="col-md-7">
			<%= render 'portfolio_main_image' %>
			<h3>Technologies Used:</h3>
			<% @portfolio_item.technologies.each do |t| %>
				<p><%= t.name %></p>
			<% end %>
			<hr>
			<a href="#">View This Project</a>
		</div>

		<div class="col-md-5">
			<h1><%= @portfolio_item.title.upcase %></h1>
			<hr>
			<em><%= @portfolio_item.subtitle %></em>
			<p><%= markdown(@portfolio_item.body) %></p>
			<hr>

			<% if has_role?(:admin) %>
				<%= link_to "Edit", edit_portfolio_path(@portfolio_item) %> /
				<%= link_to "Delete", portfolio_path(@portfolio_item), method: :delete, data: { confirm: "Are you sure?" } %>
			<% end %>
			<br>
			<%= link_to 'Back to Portfolios', portfolios_path %>
		</div>
	</div>
</div>